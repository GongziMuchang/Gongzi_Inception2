@using System.Data;
@using System.Data.SqlClient;
@using System.Text.Encodings.Web;
@using System.Web;

@{
    // get url
    //string url = HttpContext.Current.Request.Url.AbsoluteUri;

    // split url by /
    //string[] tokens = url.Split("/");

    SqlConnection con = new SqlConnection("Data Source=(localdb)\\mssqllocaldb;Initial Catalog=ConestogaCarpool;Integrated Security=True");

    con.Open();

    //String sqlString = "UPDATE Post SET StatusId = 2 WHERE PostId = " + tokens[tokens.Length - 1];

    //SqlCommand cmd = new SqlCommand(sqlString, con);

    //cmd.ExecuteNonQuery();

    con.Close();
}